apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: api
  labels:
    app: api
spec:
  selector:
    matchLabels:
      app: api
  endpoints:
    # Tên port trong Service, KHÔNG phải số.
    - port: http
      path: /metrics
      interval: 30s
