apiVersion: v2
name: elearning
description: E-learning platform API and its Postgres database
type: application

# Version CỦA CHART — tăng khi sửa template.
version: 0.1.0

# Version CỦA APP bên trong — bám theo tag image.
# Hai số này độc lập: sửa resources mà không đổi code thì chỉ version tăng.
appVersion: 'b6beff0'